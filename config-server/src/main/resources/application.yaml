spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kbabayevv/config-server
          default-label: master
          clone-on-start: true
server:
  port: 8888
